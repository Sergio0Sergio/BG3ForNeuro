# bg3-neuro-followups — post-review bucket

Tracked: 7 tickets produced from the two-axis code-review of `HEAD` -> working tree (docs translation + honest combat economy + language-independent aliases). The two cross-cutting defects found there were fixed before commit; everything else lives here.

Tracker: local markdown (`.scratch/<effort>/`), conventions of `issue-tracker-local.md`.

## Status
- [x] 01-bonus-legacy-ba-spend — done (live bench: `Osi.UseSpell` spends NO BA/AP natively; `PartyIncreaseActionResourceValue` is a no-op for personal resources; public write set = `AddActionPoints`(AP) + `PartyIncrease…`(no-op) — no single-actor BA writer exists. Decision: enforce BA budget in-router on the legacy path, engine writer impossible). **Implementation verified live** (v0.8.30, PAK v036L legacy): legacy `bonus_action` #1 accepted + marked + logged, #2/#3 REFUSED `action_failed "Bonus action already used this turn (BA budget enforced in-router)"`; **reset-on-`TurnStarted` VERIFIED live (2026-09-21, v0.8.60, Astarion honest path, real cycling combat): turn1 #1 accepted (`bzA1`), turn1 #2 REFUSED (`bzA2`), turn2 #3 accepted through the budget gate (`bzA3`, failed only on melee range; no new `bonus REFUSED` in SE log). Ticket fully resolved.**
- [x] 02-movement-manual-cost — done (v0.8.29 live bench: honest path = budget CLAMP only, `m0` gate "No movement left" when pool 0; game drains Movement natively on scripted combat movement (9→0 verified), so no manual writer needed — PartyIncrease personal no-op stands disqualified; legacy fallback dash-equivalent `AddActionPoints(actor,-1)` gated by `forceLegacy`/`legacyStable`. Evidence: move 14m→clamped 9.4m, 2nd move rejected at Movement=0; PAK v035)
- [x] 03-attack-candidate-narrowing — done (executeAttack: 2 real prototypes, weapon discriminator, real pendingCasts default)
- [x] 04-enqueue-seam — done (options table, reason threading in all 3 callers; PAK EED60034)
- [x] 05-alias-statslug-fallback — done (already fixed in HEAD 7697f11: statSlug guards vs~=""/non-ASCII/"entity" -> nil, registerAlias falls back to slug(rawName))
- [x] 06-docs-consistency — done (spec §6.4b snapshot set, map over-claim, stale GetConfig claims, research/02 aspirational markers)
- [x] 07-duplication-smells — done (extracted knownSpellCandidates + honestEnqueue, characterPartyFlags boolean|nil decode, bonusCandidates trimmed to resolved AttackType-free set; PAK 5B4925F2)
- [x] 08-enemies-faction-misclassification — **resolved (research, 2026-09-18)**: `TurnBased.CombatTeam` is a per-combat side/team GUID (not combat GUID, not faction; team-keyed turn order — the acting Astarion's team `db9418f5-…` is one side, allied tieflings are another) → discard from classifier. Real hostility: `Osi.IsAlly`/`Osi.IsEnemy(partyRef, g)` (Osi.lua:1492/1556, Brawl `== 1` pattern, truthy check). Creature filter: `ServerCharacter`/`Osi.IsCharacter` (doors = items, no ServerCharacter; Health/Stats not safe). Write-up: `research/08-enemies-hostility-relation-api.md`; fix itself is a follow-up `ready-for-agent`. Bug filed 2026-09-17 from the v0.8.34 run.
- [x] 09-ability-catalog-friendly-names — **verified live** v0.8.35 / PAK v041 (2026-09-17): `state.spells[]` carries `name` (loca + curated fallback) and `cost`; `available_actions` advertises `cast_spell: [names]`; C# `ActionRouter` rewrote `flourish → Target_OpeningAttack` in the `action_b9r1.json` trace and the cast spent **BA 1.0 → 0.0**; direct `neuro_to_bg3.json` inject with `piercing_strike` proved the Lua safety net (`Target_PiercingThrust`, AP 1.0 → 0.0); engine id passes through unrewritten. Tests 94/94. `bonus_action` stays offhand-only.
- [x] 11-false-success-on-failed-cast — **fixed + verified** v0.8.36 / PAK v042 (2026-09-17): `finalizeCast` now writes `success = not cancelled`, and `writeResult` forces `success=false` whenever an `error_code` is present. Bench: the same `Target_MainHandAttack` at AP=0 that used to return `success:true` now returns `success:false` + `cast_failed`, and the app forwards `success:false` over WS.
- [x] 14-hostility-based-enemy-classification — resolved (2026-09-18, v0.8.46): classifier now uses `Osi.IsEnemy`/`IsAlly(partyRef, g)` (callable `Osi` proxies, called directly — presence checks `x ~= nil` throw because `Osi.__index` is a lazy C resolver) + `ServerCharacter` filter for non-characters. Live gate fight: `allies`=9 (party + `wyll_1`/`zevlor_1`/`remira_1`/`aradin_1`/`barth_1`), `enemies`=8 (goblinoids/worg only), portcullis skipped (`diag.skipped_non_character`=1); `osi_hostility`=true. PAK v052. `resolved`
- [x] 13-combatant-status-effects — resolved (2026-09-18, v0.8.41): combat state emits `conditions = [{id, name, duration_left?}]` for both sides (`ServerCharacter.StatusManager.Statuses`, SE userdata enumerated via `#`/`[i]`); engine-internal statuses dropped by a stats-`Icon` gate + blacklist fallback; no `turns_left` (engine exposes none); ally `effects` renamed to `availability`. Live: `goblin_tracker_3` -> `FLANKED`, `wyll_1` -> `FEATHER_FALL (dl=29.9)`; PAK v047; tests 155/155. `resolved`
- [x] 12-english-action-descriptions — **done** (2026-09-18): all 24 `description` strings in `src/BG3Neuro.Core/Actions/action_schemas.json` translated to English (names/schemas/`internal` unchanged). Two tests added — `Get_ActionDescriptions_AreEnglish_NoCyrillic` (registered actions) and `EmbeddedSchema_HasNoCyrillic_EvenForInternalEntries` (whole embedded file, internal entries included); `dotnet test` **155/155**. Live `actions/register` frame (App rebuilt, `bench_config.json`) has no `\u04xx` escapes, 17 actions, 0 Cyrillic descriptions. Sweep of the rest of the Neuro-facing surface clean: no user/model-facing Russian string in `src/` (Cyrillic there is `///`/`//` comments only; test project's Russian strings are fixtures/assert messages); Lua Cyrillic is comments + the intentional `translitMap` only.
- [x] 15-displayname-osi-guard — resolved (2026-09-18, v0.8.47, PAK v053): added shared `osiDisplayNameOf(guid)` (one-time warm-up + direct pcall of `Osi.GetDisplayName`), replaced both always-false `type(Osi.GetDisplayName) == "function"` guards (`probeGameState` current_characters + `displayName` fallback). Live: SE log shows the mod's `[DIV query] GetDisplayName` now executing (`ResStr_*` returned, no throw), `state_capture` sc9 `success:true` allies=9/enemies=8 with all names human-readable, no Lua errors. Fallback returns the DisplayName resource key (component path always preferred, so state names stay human). `resolved`
- [x] 10-action-count-test-drift — **done** (2026-09-17): all 7 dev/bench entries (`state_capture`, `end_turn_ecs`, `probe`, `diag_skip`, `bench_*`) marked `"internal": true` in `action_schemas.json` and skipped by `ActionRegistry.Load`, so the Neuro surface is **17 gameplay actions** (was 24; the old tests' 21 wrongly counted 4 dev actions as gameplay). Direct `neuro_to_bg3.json` injects still execute them. Tests green **153/153**; `DEVELOPER.md` documents the flag (and its `"parameters"` → `"schema"` example was fixed).
- [x] 16-osiris-cast-self-on-ally-buff — **resolved + implemented + live-verified** (2026-09-18/19): "self-cast" hypothesis falsified (recipient-scoped SE events always carried the ally guid); real cause — friendly single-target buffs via osiris queue need the force option set. `executeCast` auto-enables `force_flags` for non-enemy targets (v0.8.49, PAK v055→v056 after a broken-build lesson: PAK must mirror `meta.lsx` + `ScriptExtender/` layout). Live: Guidance → `StatusApplied`,760 without flags; enemy FireBolt unchanged. See `issues/16-osiris-cast-self-on-ally-buff.md`.
- [x] 17-cast-file-bridge-spell-name-transient — **resolved (harness, not mod)**: child `powershell -File -Data` drops quotes; fix = env-var `BG3NEURO_DATA` + JSON self-validation in `drive_action.ps1` (AGENTS.md rule).
- [x] 18-force-flags-cast-ap-economy — **resolved, full honesty matrix live-verified** (2026-09-19, v0.8.50–0.8.55): option A — AP/BA/slot pre-gates + post-success deduction (AP via `Osi.AddActionPoints`, slots/BA via direct `ActionResources.Resources[uuid][i].Amount` write; pool UUIDs from Shared.pak; Osiris has no personal writer). Legs: Bless AP−1/slot−1, Healing Word BA−1/slot−1 (heal 9→15), useOsiSpell FireBolt AP−1, gate refusals (`no_spell_slot`/`no_action_point`), book-guard (`no_spell`). See `issues/18-force-flags-cast-ap-economy.md`.
- [x] 19-aoe-autonomous-target — **resolved 2026-09-19, residual closed 2026-09-21** (v0.8.56–0.8.57): router `FillAoEPosition` (auto-center inject, 4 unit tests) + mod Z-fallback + Zone `Range`/`Base` emitter fix + group-turn casting via stack (exact `availability` match) + Zone no-target guard + `useOsiSpell` book-guard. Live: Thunderwave killed goblin_tracker_2 (file bridge, v98t1) and goblin_tracker_1 (stack group cast, t99-7; AP/slot deducted). **Residual closed live (2026-09-21, solo Gale turn, PAK v091 / v0.8.60)**: (a) emitter `aoe=5.0 range=5.0` VERIFIED on Gale's turn (state_capture c19cap); (b) `FillAoEPosition` auto-inject E2E VERIFIED via official Randy (neuro-sdk, WS :8000, action POST :1337) — inject cast_spell thunderwave with no position → router injected `targetPos [215.3,31.7,408.75]` (z via caster fallback), mod accepted (WS success:true); per-action result was `cast_failed` only because Gale had AP=0 at inject time (engine interrupt; no resources/HP touched — not a defect). PAK v062→v065 across the day; tests 163/163.

## Fixed before commit (not tickets)
- auto `insertAtFront` now uses `detectIsPlayer` (ServerCharacter) instead of `actor:find("Player")` — `executeCast`.
- empty TODO "manual BA spend on legacy fallback" removed from `executeBonusAction` (dead block) — its real behavior gap became ticket 01.
- [x] 22-in-range-target-substitution — **resolved (server truth, not self-cast)** (2026-09-21, v090): raw StatusManager via `stats_probe` proved the in-range friendly cast lands on the REQUEST target at EVERY flag/queue combo — soft osiris (c16a Bless on Tav), soft network (c20n Guidance on Tav, 60 s), hard osiris `Forced/Immediate` + `force_flags:true` (c23g2, re-run after stale `GameplayControllerCancelRequests` aborted the first attempt with resources untouched). The caster-side "icon" on Bless/Guidance is the CONCENTRATION indicator, not the status recipient; `bg3_to_neuro` combatant inventory (turn-holder) and `stats_probe` are the only truths (CONTEXT.md Diagnostic traps). Original symptom fully falsified. **Closed 2026-09-21 (v0.8.61):** optional >2 m control — `bless` cleric→tav at 4.6 m, `stats_probe` after: `tav [RALLY, BLESS]`, `cleric [RALLY]` (AP 1.0→0.0, slot L1 1.0→0.0); UX decision: caster-side icon/projectile = cosmetic render artifact (CONTEXT.md → Diagnostic traps). `resolved`. See issues/22-in-range-target-substitution.md
- [x] 20-multitarget-bless-cast — **resolved, live-verified** (2026-09-21, v0.8.60, PAK v091): `cast_spell` accepts `target_ids: [..]` (single `target_id` stays back-compat). Lua `executeCast` merges both into `targetIds`, pre-validates range+LOS for EVERY member, sets auto-force only when all targets are known non-enemies, honestly refuses `use_osi_spell` multi; `enqueueCastRequest` builds `Targets[]` from the full list — ONE engine cast, one slot/AP. C# router validates each member, `FillAoEPosition` skips when `target_ids` present. 166/166 tests. **Live bench (gate, Shadowheart):** `target_ids:[tav, astarion, wizard]` → `success:true`, `BLESS` confirmed on all 3 targets via `stats_probe` (raw StatusManager, dl=60), SpellSlot lvl1 **1.0→0.0**, AP **1.0→0.0**, BA/Movement untouched, no Lua errors. See issues/20-multitarget-bless-cast.md
- [x] 21-combat-capture-cost — **resolved** (2026-09-21, v0.8.57+): combat `TurnStarted` emission cost (ticket 05 threshold: median ≤ 20 ms over 5+ runs) re-measured on the CURRENT combat frame vs the 20 ms threshold. 86 samples from the cleared cache + today's gate: **median 9.04 ms, mean 9.61 ms, min 5.01 ms, max 24.1 ms**; 16/86 fell in the 12–20 ms band, 1/86 exceeded 20 ms, none approached the threshold after cache-warming. No optimization needed — close. See issues/21-combat-capture-cost.md
- [x] 23-remove-seen-by — **verified (v0.8.62, PAK v093 installed + live)** (2026-09-21): perception contract rev 2 said `seen_by` is removed, but the mod still emitted `seen_by="player"` (`BG3Neuro.lua:2842`) and the C# **did** read it (serializer grouped by `SeenBy` → `## Objects (seen by …, N)`). Removed the emission, deleted `ExplorationObject.SeenBy`, serializer now emits a single `## Objects (N)` (cap + "… and N more" preserved over the whole list), tests/docs updated. luaparse OK; 166/166; build clean. PAK v093 (88437 B, MD5 `16caabb149309eb6387971d8f0eb5d57`) installed, MD5 updated in both `ModuleShortDesc`; live: heartbeat `0.8.62`, `bg3_to_neuro.json` has no `seen_by`. See issues/23-remove-seen-by.md
- [x] 24-invisible-b-gate — **verified (v0.8.63, PAK v095 installed + live)** (2026-09-21): P6 v2 — exploration B-gate now drops masked entities. New `BG3NEURO_SIGHT` namespace (`isMasked`: target has `INVISIBLE`/`SNEAKING` and leader lacks `TRUESIGHT`/see-invisibility ≤9 m; raw `StatusMachine`, fail-open) wired into `sightSees` after a positive LOS verdict; bench-only `perception_probe` action (dumps `los`/`is_invisible`/`masked`/`statuses[]`, optional `apply_status`/`remove_status`). Live: baseline `masked=[]` (18 objects = the `los=true` rows; ~20 `is_invisible:1` entities with no masking status correctly NOT masked — spec §2 trap), `INVISIBLE` → `masked=["Ogre Brute"]` + `objects` 18→17, remove → 18; `SNEAKING` → masked; `TRUESIGHT` on leader → exemption (`masked=[]`). Server `v0.8.63 loaded`, no Lua errors; dispatch median 11.1 ms. Trap: main chunk had exactly 200 active locals (`MAXVARS`) — first attempt (7 new top-level `local`s) failed to parse and the module silently vanished; fixed via global namespace (headroom 0). See issues/24-invisible-b-gate.md
